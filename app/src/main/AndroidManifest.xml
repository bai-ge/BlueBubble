<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.daimao.bluebubble">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.LockActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"
                  android:launchMode="singleInstance"
                  android:windowSoftInputMode="adjustPan|stateHidden"/>
        <activity android:name=".example.TestActivity"></activity>
        <activity android:name=".example.StaggeredGridActivity"/>
        <activity android:name=".example.StaggeredGridActivityFragment"/>
        <activity android:name=".example.StaggeredGridEmptyViewActivity"/>
        <activity android:name=".example.ListViewActivity"/>
    </application>

</manifest>